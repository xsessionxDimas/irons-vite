export type LookupSiteItem = {
  siteId: string;
  siteName: string;
}[];

export type LookupOwnershipItem = {
  ownershipId: number;
  ownership: string;
}[];

export type LookupEqpNoItem = {
  equipmentNoId: number;
  equipmentNo: string;
}[];

export type LookupEqpModelItem = {
  equipmentModelId: number;
  equipmentModel: string;
}[];

export type LookupEqpGroupItem = {
  equipmentGroupId: number;
  equipmentGroup: string;
}[];

export type LookupCompItem = {
  componentId: number;
  componentCode: string;
  componentName: string;
}[];

export type LookupEqpBrandItem = {
  brandId: number;
  brand: string;
}[];
