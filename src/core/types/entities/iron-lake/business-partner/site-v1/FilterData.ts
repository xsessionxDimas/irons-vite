export type FilterData = {
    SiteId: string
    StartDate: string,
    EndDate: string,
    SiteIdTo: string
    StartDateTo: string,
    EndDateTo: string,
    Page: number,
    PageSize: number,
    Order: string,
    ver: string
}
