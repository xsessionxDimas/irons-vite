export type ListItem = {
    SiteKeyId: number,
    SiteId: string,
    SiteDescription: string,
    SiteCode: string,
    StartDate: string,
    EndDate: string,
    IsActive: boolean,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
