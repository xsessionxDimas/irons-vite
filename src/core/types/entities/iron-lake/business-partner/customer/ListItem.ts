export type ListItem = {
    customerId: number,
    customer: string,
    customerDescription: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
