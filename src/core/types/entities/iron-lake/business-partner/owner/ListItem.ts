export type ListItem = {
    ownerId: number,
    owner: string,
    ownerDescription: string,
    startDate: string,
    endDate: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
