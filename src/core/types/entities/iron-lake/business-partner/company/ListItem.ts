export type ListItem = {
    CompanyId: number,
    Company: string,
    CompanyDescription: string,
    StartDate: string,
    EndDate: string
    IsActive: boolean,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
