export type FilterData = {
    description: string,
    equipmentModelId: string | number | null,
    serviceSize: string,
    typeParameterId: string | number | null,
    componentId: string | number | null,
    cbmGroupId: string | number | null,
    rating: string,
    isSync: boolean,
    page: number,
    pageSize: number,
}
