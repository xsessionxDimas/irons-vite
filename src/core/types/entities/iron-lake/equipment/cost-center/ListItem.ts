export type ListItem = {
    costCenterId: number,
    costCenter: string,
    costCenterDescription: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
