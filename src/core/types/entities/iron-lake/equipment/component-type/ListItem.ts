export type ListItem = {
    componentTypeId: number,
    componentType: string,
    componentTypeDescription: string,
    startDate: string,
    endDate: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
