export type ListItem = {
    levelId: number,
    level: string,
    parent: string,
    startDate: string,
    endDate: string,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
