export type ListItem = {
    statusId: number,
    systemStatus: string,
    status: string,
    subStatus: string,
    startDate: string,
    endDate: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
