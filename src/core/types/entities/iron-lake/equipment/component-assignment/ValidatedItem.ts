export type ValidatedItem = {
    componentType: string,
    component: string,
    equipment: string,
    objectType: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
