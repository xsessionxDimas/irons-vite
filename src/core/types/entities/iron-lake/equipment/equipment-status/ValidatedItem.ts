export type ValidatedItem = {
    equipment: string,
    systemStatus: string,
    status: string,
    subStatus: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
