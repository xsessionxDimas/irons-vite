export type ListItem = {
    equipmentStatusId: number,
    equipment: string,
    systemStatus: string,
    status: string,
    subStatus: string,
    startDate: string,
    endDate: string,
    isActive: boolean,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
