export type ValidatedItem = {
    mdTypeParameterId : number,
    idParameter : string,
    parameter : string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string,
}
