export type ValidatedItem = {
    mdSMUToleranceSettingId: number,
    parameter: string,
    valueMin: string,
    valueMax: string,
    uom: string,
    uomId: number,
    isActive: boolean,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string,
}
