export type ValidatedItem = {
    value: string,
    valueMin: string,
    valueMax: string,
    uom: string
    startDate: string
    endDate: string
    isActive: true
    isValid: boolean,
    validationReason: string
}
