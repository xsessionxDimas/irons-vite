export type ListItem = {
    oilToleranceSettingId: number,
    value: string,
    valueMin: string
    valueMax: string,
    uom: string,
    startDate: string,
    endDate: string,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string,
    isActive: boolean
}
