export type ListItem = {
    mdUomId: string,
    uom: string
    startDate: string,
    endDate: string,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string,
    isActive: boolean
}
