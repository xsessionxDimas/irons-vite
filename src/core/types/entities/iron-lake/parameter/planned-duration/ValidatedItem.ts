export type ValidatedItem = {
    model: string,
    psType: string,
    value: string,
    uom: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string,
}
