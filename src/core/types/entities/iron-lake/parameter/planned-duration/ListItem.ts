export type ListItem = {
    plannedDurationId : number,
    model : string,
    psType : string,
    value : string,
    uom : string,
    startDate : string,
    endDate : string,
    isActive : true,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
