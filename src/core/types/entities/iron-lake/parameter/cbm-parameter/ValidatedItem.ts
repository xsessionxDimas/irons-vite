export type ValidatedItem = {
    mdCbmParameterId: string,
    cbmParameter: string,
    startDate: string
    endDate: string
    isActive: true
    isValid: boolean,
    validationReason: string
}
