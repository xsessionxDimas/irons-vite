export type ListItem = {
    locationCrackId: number,
    locationId: string,
    locationDescription: string,
    isActive: boolean,
    startDate: string,
    endDate: string,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
