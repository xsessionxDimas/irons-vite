export type ImageInfo = {
  filename: string,
  originalFilename?: string,
  description: string,
  type?: string,
  fileType?: string
}

export type ImageFull = {
  imgBlob: string,
  url: string,
  description: string
}
