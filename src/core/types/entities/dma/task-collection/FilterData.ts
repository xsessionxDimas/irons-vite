export type FilterData = {
  modelId: string;
  psTypeId: string;
  version: string;
  releaseDate: string,
  category: string;
  description: string;
  subTask: string;
  status: string;
  page: number;
  pageSize: number;
};
