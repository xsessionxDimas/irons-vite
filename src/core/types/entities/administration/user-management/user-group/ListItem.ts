export type ListItem = {
    UserGroupId: number,
    GroupName: string,
    GroupDescription: string,
    Site: string,
    ChangedOn: Date,
    ChangedBy: string
    CreatedBy: string
    CreatedOn: Date
}
