export type ListItem = {
    LanguageId: number,
    Code: string,
    ValueId: string,
    ValueEn: string,
    StartDate: string,
    EndDate: string,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
