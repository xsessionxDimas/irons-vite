export type ListItem = {
    PositionId: number,
    Position: string,
    PositionDescription: string,
    IsDma: boolean,
    StartDate: string,
    EndDate: string,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
