export type FilterData = {
    Position: string
    PositionDescription: string
    StartDate: string | null,
    EndDate: string | null,
    IsDma: boolean | null,
    ver: string,
    Page: number,
    PageSize: number,
    Order: string
}
