export type FilterData = {
    site: string,
    group: string,
    model: string,
    equipment: string[],
    component: string[],
    startDate: string,
    endDate: string,
    page: number,
    pageSize: number,
    order: string,
    ver: string,
}
