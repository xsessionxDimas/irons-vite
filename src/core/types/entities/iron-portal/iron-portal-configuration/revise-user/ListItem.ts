export type ListItem = {
    reviseUserId: number,
    employeeId: string,
    reviseUser: string,
    startDate: string,
    endDate: string,
    isActive: number,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
