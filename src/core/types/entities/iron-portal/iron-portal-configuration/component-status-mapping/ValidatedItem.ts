export type ValidatedItem = {
    site: string,
    equipmentModel: string,
    cbmGroup: string,
    componentStatusId: string,
    objectType: string,
    group: string,
    rating: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
