export type ValidatedItem = {
    site: string,
    equipmentModel: string,
    objectType: string,
    codeGroup: string,
    damageCategory: string,
    damage: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
