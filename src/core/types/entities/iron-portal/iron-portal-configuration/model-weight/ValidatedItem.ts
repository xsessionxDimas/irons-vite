export type ValidatedItem = {
    site: string,
    equipmentModel: string,
    weight: string,
    whPerday: string,
    fuelBurn: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
