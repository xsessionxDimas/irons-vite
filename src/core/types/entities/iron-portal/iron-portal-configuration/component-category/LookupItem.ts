export type LookupItem = {
    type: string[],
    sequential: string[],
    operator: string[],
    valueMin: string[],
    valueMax: string[],
    uom: string[],
    category: string[],
    categoryDesc: string[]
}
