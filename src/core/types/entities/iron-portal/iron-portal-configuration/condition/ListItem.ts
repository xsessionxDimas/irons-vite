export type ListItem = {
    ConditionId: number,
    Condition: string,
    ConditionDescription: string,
    StartDate: string,
    EndDate: string,
    IsActive: number,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
