export type ValidatedItem = {
    equipmentModel: string,
    objectType: string,
    cbmGroup: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
