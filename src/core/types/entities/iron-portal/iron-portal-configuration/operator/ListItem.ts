export type ListItem = {
    OperatorId: number,
    Operator: string,
    OperatorDescription: string,
    StartDate: string,
    EndDate: string,
    IsActive: number,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
