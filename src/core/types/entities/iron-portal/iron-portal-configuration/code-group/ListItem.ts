export type ListItem = {
    CodeGroupId: number,
    CodeGroup: string,
    CodeGroupDescription: string,
    StartDate: string,
    EndDate: string,
    IsActive: number,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
