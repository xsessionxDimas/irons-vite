export type ValidatedItem = {
    ratingParameter: string,
    ratingCode: string,
    ratingDescription: string,
    startDate: string,
    endDate: string,
    isValid: boolean,
    validationReason: string
}
