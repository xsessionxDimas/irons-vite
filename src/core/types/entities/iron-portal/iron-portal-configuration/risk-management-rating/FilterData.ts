export type FilterData = {
    ratingParameter: string,
    ratingParameterTo: string,
    ratingCode: string,
    ratingCodeTo: string,
    startDate: string,
    startDateTo: string,
    endDate: string,
    endDateTo: string,
    page: number,
    pageSize: number,
    order: string,
    ver: string,
}
