export type ListItem = {
    managementRatingId: number,
    ratingParameter: string,
    ratingCode: string,
    ratingDescription: string,
    startDate: string,
    endDate: string,
    isActive: number,
    createdOn: Date,
    createdBy: string,
    changedOn: Date,
    changedBy: string
}
