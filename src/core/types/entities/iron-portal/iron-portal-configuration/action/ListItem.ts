export type ListItem = {
    ActionId: number,
    Action: string,
    ActionDescription: string,
    StartDate: string,
    EndDate: string,
    IsActive: number,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
