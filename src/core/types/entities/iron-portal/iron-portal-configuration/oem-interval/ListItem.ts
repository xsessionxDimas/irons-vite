export type ListItem = {
    OemIntervalId: number,
    IdOemInterval: string,
    Interval: string,
    Value: string,
    StartDate: string,
    EndDate: string,
    IsActive: number,
    CreatedOn: Date,
    CreatedBy: string,
    ChangedOn: Date,
    ChangedBy: string
}
