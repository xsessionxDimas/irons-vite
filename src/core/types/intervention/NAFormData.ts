import { TaskUpdateObject } from './TaskUpdateObject'

export type NAFormData = {
   reason: string,
   reasonDesc: string,
   taskNo: string,
   taskDesc: string
   task: TaskUpdateObject
   submitDate: string
}
