export type TaskImages = {
    taskKey: string,
    type: string,
    sapWorkorder: string,
    updatedBy: string,
    email: string,
    filename: string,
    image: Blob,
    status: string,
    fileType?: string,
    syncDate?: string,
}
